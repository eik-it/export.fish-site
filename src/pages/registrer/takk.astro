---
import Layout from '../../layouts/Layout.astro';
---

<Layout
  title="Takk - export.fish"
  description="Vi har mottatt din henvendelse og tar kontakt med deg snart for å hjelpe deg i gang med export.fish."
  noindex
>
  <!-- Hero Section -->
  <section class="pt-52 pb-16 bg-gradient-to-br from-blue-50 to-blue-100">
    <div class="container mx-auto px-4 max-w-4xl text-center">
      <!-- Success Icon -->
      <div class="mb-6">
        <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto">
          <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
        </div>
      </div>

      <h1 id="takk-title" class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
        Takk for din interesse!
      </h1>
      <p id="takk-subtitle" class="text-xl text-gray-600 max-w-2xl mx-auto">
        Vi har mottatt din henvendelse og tar kontakt med deg snart.
      </p>
    </div>
  </section>

  <!-- Content Section -->
  <section class="py-16 bg-white">
    <div class="max-w-3xl mx-auto px-4">
      <!-- Interest confirmation (default) -->
      <div id="interest-content" class="bg-white rounded-2xl shadow-lg border border-gray-200 p-8 md:p-12">
        <h2 class="text-xl font-semibold text-gray-900 mb-6">Hva skjer nå?</h2>
        <ul class="space-y-6">
          <li class="flex items-start gap-4">
            <div class="w-8 h-8 bg-green-100 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
              <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <div>
              <p class="font-medium text-gray-900">Din henvendelse er mottatt</p>
              <p class="text-sm text-gray-600">Vi har registrert din interesse for export.fish og er glad for at du vil vite mer.</p>
            </div>
          </li>
          <li class="flex items-start gap-4">
            <div class="w-8 h-8 bg-primary/10 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
              <span class="text-primary font-semibold text-sm">1</span>
            </div>
            <div>
              <p class="font-medium text-gray-900">Vi tar kontakt innen 1–2 virkedager</p>
              <p class="text-sm text-gray-600">En av oss i teamet vil kontakte deg på e-post eller telefon for å svare på spørsmål og hjelpe deg i gang.</p>
            </div>
          </li>
          <li class="flex items-start gap-4">
            <div class="w-8 h-8 bg-primary/10 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
              <span class="text-primary font-semibold text-sm">2</span>
            </div>
            <div>
              <p class="font-medium text-gray-900">Vi setter opp alt du trenger</p>
              <p class="text-sm text-gray-600">Når du er klar, hjelper vi deg med registreringen slik at du raskt kan komme i gang med digital fangstrapportering.</p>
            </div>
          </li>
        </ul>

        <!-- Spam notice -->
        <div class="mt-6 pt-6 border-t border-gray-100 bg-amber-50 -mx-4 px-4 py-4 rounded-lg">
          <p class="text-sm text-gray-700">
            <strong class="text-gray-900">Tips:</strong> Sjekk søppelpost/spam-mappen hvis du ikke finner e-post fra oss.
          </p>
        </div>
      </div>

      <!-- Full registration confirmation (shown when ?type=registrert) -->
      <div id="registrert-content" class="hidden bg-white rounded-2xl shadow-lg border border-gray-200 p-8 md:p-12">
        <h2 class="text-xl font-semibold text-gray-900 mb-6">Hva skjer nå?</h2>
        <ul class="space-y-6">
          <li class="flex items-start gap-4">
            <div class="w-8 h-8 bg-green-100 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
              <svg class="w-4 h-4 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <div>
              <p class="font-medium text-gray-900">Registreringen er mottatt</p>
              <p class="text-sm text-gray-600">Vi har mottatt all informasjon og oppretter din konto.</p>
            </div>
          </li>
          <li class="flex items-start gap-4">
            <div class="w-8 h-8 bg-primary/10 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
              <span class="text-primary font-semibold text-sm">1</span>
            </div>
            <div>
              <p class="font-medium text-gray-900">Kontoen din aktiveres</p>
              <p class="text-sm text-gray-600">Vi verifiserer informasjonen og setter opp ditt export.fish-subdomene. Du mottar en e-post når alt er klart.</p>
            </div>
          </li>
          <li class="flex items-start gap-4">
            <div class="w-8 h-8 bg-primary/10 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
              <span class="text-primary font-semibold text-sm">2</span>
            </div>
            <div>
              <p class="font-medium text-gray-900">Del lenken med turistene</p>
              <p class="text-sm text-gray-600">Når kontoen er aktiv, kan du dele din unike export.fish-lenke med turistene slik at de kan registrere fangst selv.</p>
            </div>
          </li>
        </ul>

        <!-- Spam notice -->
        <div class="mt-6 pt-6 border-t border-gray-100 bg-amber-50 -mx-4 px-4 py-4 rounded-lg">
          <p class="text-sm text-gray-700">
            <strong class="text-gray-900">Tips:</strong> Sjekk søppelpost/spam-mappen hvis du ikke finner e-post fra oss.
          </p>
        </div>
      </div>

      <!-- Contact info -->
      <div class="mt-12 text-center">
        <p class="text-gray-600 mb-6">
          Har du spørsmål i mellomtiden?
        </p>
        <a
          href="/kontakt"
          class="inline-flex items-center gap-2 bg-primary hover:bg-primary-hover text-white font-semibold py-3 px-6 rounded-lg transition-colors"
        >
          Kontakt oss
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>
</Layout>

<script>
  const urlParams = new URLSearchParams(window.location.search);
  const type = urlParams.get('type');

  if (type === 'registrert') {
    // Show full registration confirmation
    const title = document.getElementById('takk-title');
    const subtitle = document.getElementById('takk-subtitle');
    const interestContent = document.getElementById('interest-content');
    const registrertContent = document.getElementById('registrert-content');

    if (title) title.textContent = 'Registreringen er fullført!';
    if (subtitle) subtitle.textContent = 'Takk for at du valgte export.fish. Vi setter opp kontoen din nå.';
    if (interestContent) interestContent.classList.add('hidden');
    if (registrertContent) registrertContent.classList.remove('hidden');
  }
</script>
